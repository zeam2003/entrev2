const sessionConfig = {
    secret: 'soyunomasdelmonton',
        resave: false,
        saveUninitialized: false,
        cookie: {
            maxAge: 60000
        },
}

export default sessionConfig;